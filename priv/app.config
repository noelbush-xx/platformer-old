[{platformer, [
               {ip, "0.0.0.0"},
               {port, 8000},
               {log_dir, "log"},            % relative to the priv directory
               {dispatch, "dispatch.conf"}, % relative to the priv directory
               {servers, ["http://0.0.0.0:8000"]}
              ]
 }%%,
 %% {sasl, [
 %%         {sasl_error_logger, false},
 %%         {errlog_type, all},
 %%         {error_logger_mf_dir, "/home/noel/workspace/platformer/priv/log"},
 %%         {error_logger_mf_maxbytes, 10485760}, % 10MB
 %%         {error_logger_mf_maxfiles, 10}
 %%        ]
 %% }].
].
